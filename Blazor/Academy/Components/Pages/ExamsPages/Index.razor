@page "/exams"
@using Microsoft.AspNetCore.Components.QuickGrid
@using Microsoft.EntityFrameworkCore
@using Academy.Data.Models
@implements IAsyncDisposable
@inject IDbContextFactory<AcademyContext> DbFactory
@inject NavigationManager NavigationManager

@rendermode InteractiveServer

<PageTitle>Exams</PageTitle>

<h1>Exams</h1>

<p>
    <a href="/exams/create">Create New</a>
</p>

<div class="controls">

    <div>
        <label for="items-per-page">Items per page:</label>
        <select id="items-per-page" @bind="pagination.ItemsPerPage">
            <option value="3">3</option>
            <option value="5">5</option>
            <option value="8">8</option>
            <option value="10">10</option>
        </select>
    </div>
</div>

<div>
    <QuickGrid Class="table" Items="context.Exams" Pagination="pagination">
<PropertyColumn Property="m => m.Student" Sortable="true" />
        <PropertyColumn Property="m => m.Discipline" Sortable="true" />
        <PropertyColumn Property="m => m.Date" Sortable="true" />
        <PropertyColumn Property="m => m.Grade" Sortable="true" />
        <TemplateColumn Context="m">
            <a href="@($"exams/details?student={m.Student}&discipline={m.Discipline}")">Details</a> |
            <a href="@($"exams/edit?student={m.Student}&discipline={m.Discipline}")">Edit</a> |
            <a href="@($"exams/delete?student={m.Student}&discipline={m.Discipline}")">Delete</a>
        </TemplateColumn>
    </QuickGrid>
    <Paginator State="pagination" />
</div>

@code {
    private AcademyContext context = default!;


    PaginationState pagination = new PaginationState { ItemsPerPage = 10 };

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}